/// <summary>装备信息的取值类型</summary>
public enum EquipmentInfoType
{
    /// <summary>是否有装备：1=有，0=无</summary>
    HasEquipment,
    AttackBonus,
    MaxHealthBonus,
    AttackDistanceBonus,
    StaminaBonus,
    ActionPointBonus,
}
